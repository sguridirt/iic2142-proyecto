<h1>Solicitudes</h1>

<table class="table">
  <thead>
    <tr>
      <th>Curso</th>
      <th>Estudiante</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @requests.each do |request| %>
      <tr>
        <td><%= request.course.title if request.course.present? %></td>
        <td><%= request.student.user.name if request.student.present? %></td>
        <td>
          <!-- Aquí puedes agregar botones para aprobar/rechazar solicitudes -->
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
